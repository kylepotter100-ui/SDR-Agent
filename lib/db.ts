/**
 * Supabase client wrapper.
 *
 * Stub for Checkpoint 1. Real client is added in Checkpoint 2 once the
 * Supabase project is connected via the Supabase MCP and the initial
 * schema is migrated. All database access in the agent goes through
 * this single module — never import @supabase/supabase-js elsewhere.
 */

export function db(): never {
  throw new Error(
    "lib/db.ts is not implemented yet. Wire up the Supabase client in Checkpoint 2.",
  );
}
