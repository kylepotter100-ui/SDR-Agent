export default function SettingsPage() {
  return (
    <div>
      <h1 className="font-serif text-2xl tracking-tight text-brand-near-black">
        Settings
      </h1>
      <p className="mt-2 text-sm text-brand-near-black/60">
        In development — read-only display of postcode prefixes and SIC tier
        weights lands in a later checkpoint.
      </p>
    </div>
  );
}
